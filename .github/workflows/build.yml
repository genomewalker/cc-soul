name: Build

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

env:
  ONNX_VERSION: "1.16.3"
  DUCKDB_VERSION: "1.1.3"

jobs:
  build-linux:
    runs-on: ubuntu-latest
    container:
      image: quay.io/pypa/manylinux2014_x86_64

    steps:
      - uses: actions/checkout@v4

      - name: Install build tools
        run: |
          yum install -y wget perl-IPC-Cmd
          # Install newer cmake (manylinux2014 has old one)
          pip3 install cmake ninja

      - name: Download and build ONNX Runtime
        run: |
          # Download pre-built ONNX Runtime
          cd /tmp
          wget -q https://github.com/microsoft/onnxruntime/releases/download/v${{ env.ONNX_VERSION }}/onnxruntime-linux-x64-${{ env.ONNX_VERSION }}.tgz
          tar xzf onnxruntime-linux-x64-${{ env.ONNX_VERSION }}.tgz
          mkdir -p /opt/onnxruntime
          cp -r onnxruntime-linux-x64-${{ env.ONNX_VERSION }}/* /opt/onnxruntime/

      - name: Build DuckDB from source
        run: |
          git clone --depth 1 --branch v${{ env.DUCKDB_VERSION }} https://github.com/duckdb/duckdb.git
          cd duckdb
          mkdir build && cd build
          cmake .. -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_SHELL=OFF \
            -DBUILD_UNITTESTS=OFF \
            -DENABLE_SANITIZER=OFF \
            -DENABLE_UBSAN=OFF
          ninja
          mkdir -p /opt/duckdb/{lib,include}
          cp src/libduckdb.so /opt/duckdb/lib/
          cp -r ../src/include/duckdb* /opt/duckdb/include/

      - name: Install nlohmann_json
        run: |
          cd /tmp
          wget -q https://github.com/nlohmann/json/releases/download/v3.11.3/json.hpp
          mkdir -p /opt/nlohmann_json/include/nlohmann
          mv json.hpp /opt/nlohmann_json/include/nlohmann/

      - name: Build chitta
        run: |
          mkdir -p chitta/build bin
          cd chitta/build
          cmake .. -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_RPATH="\$ORIGIN" \
            -DCMAKE_BUILD_WITH_INSTALL_RPATH=ON \
            -DDUCKDB_INCLUDE_DIR=/opt/duckdb/include \
            -DDUCKDB_LIB_DIR=/opt/duckdb/lib \
            -DONNXRUNTIME_INCLUDE_DIR=/opt/onnxruntime/include \
            -DONNXRUNTIME_LIB_DIR=/opt/onnxruntime/lib \
            -DJSON_INCLUDE_DIR=/opt/nlohmann_json/include
          ninja

      - name: Verify binaries
        run: |
          ls -la bin/
          for b in chitta chittad chitta_migrate chitta_import duckdb_test; do
            test -x bin/$b || { echo "Missing: $b"; exit 1; }
          done

      - name: Package binaries
        run: |
          mkdir -p dist
          # Bundle required libraries
          cp /opt/onnxruntime/lib/libonnxruntime.so.${{ env.ONNX_VERSION }} bin/
          ln -sf libonnxruntime.so.${{ env.ONNX_VERSION }} bin/libonnxruntime.so
          cp /opt/duckdb/lib/libduckdb.so bin/
          # Set RPATH for all binaries
          for b in bin/chitta bin/chittad bin/chitta_migrate bin/chitta_import bin/duckdb_test; do
            patchelf --set-rpath '$ORIGIN' $b 2>/dev/null || true
          done
          # Verify
          echo "Checking library dependencies..."
          for b in bin/chitta bin/chittad; do
            echo "=== $b ==="
            ldd $b | grep -E "not found" || echo "All libs found"
          done
          tar -czvf dist/chitta-linux-x64.tar.gz -C bin .

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: chitta-linux-x64
          path: dist/chitta-linux-x64.tar.gz

  models:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Download ONNX model
        run: |
          pip install huggingface_hub
          python -c "
          from huggingface_hub import hf_hub_download
          import shutil
          import os

          # Download all-MiniLM-L6-v2 ONNX model
          model_path = hf_hub_download(
              repo_id='sentence-transformers/all-MiniLM-L6-v2',
              filename='onnx/model.onnx'
          )
          vocab_path = hf_hub_download(
              repo_id='sentence-transformers/all-MiniLM-L6-v2',
              filename='vocab.txt'
          )

          os.makedirs('models', exist_ok=True)
          shutil.copy(model_path, 'models/model.onnx')
          shutil.copy(vocab_path, 'models/vocab.txt')
          print(f'Model: {os.path.getsize(\"models/model.onnx\")} bytes')
          print(f'Vocab: {os.path.getsize(\"models/vocab.txt\")} bytes')
          "

      - name: Package models
        run: tar -czvf onnx-models.tar.gz -C models .

      - name: Upload models
        uses: actions/upload-artifact@v4
        with:
          name: onnx-models
          path: onnx-models.tar.gz

  build-macos:
    strategy:
      matrix:
        include:
          - os: macos-latest
            arch: macos-arm64
            conda_subdir: osx-arm64
          - os: macos-15-intel
            arch: macos-x64
            conda_subdir: osx-64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: "3.11"
          channels: conda-forge
          channel-priority: strict
          conda-remove-defaults: true

      - name: Install dependencies
        shell: bash -l {0}
        run: |
          CONDA_SUBDIR=${{ matrix.conda_subdir }} conda install -y cmake nlohmann_json sqlite onnxruntime-cpp=${{ env.ONNX_VERSION }} libduckdb-devel

      - name: Build chitta
        shell: bash -l {0}
        run: |
          mkdir -p chitta/build bin
          cd chitta/build
          cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_RPATH="@executable_path" \
            -DCMAKE_BUILD_WITH_INSTALL_RPATH=ON \
            -DONNXRUNTIME_INCLUDE_DIR=$CONDA_PREFIX/include \
            -DONNXRUNTIME_LIB_DIR=$CONDA_PREFIX/lib
          make -j$(sysctl -n hw.ncpu)

      - name: Verify binaries
        run: |
          ls -la bin/
          for bin in chitta chittad chitta_migrate chitta_import duckdb_test; do
            test -x bin/$bin || { echo "Missing: $bin"; exit 1; }
          done

      - name: Run tests
        shell: bash -l {0}
        run: |
          cd chitta/build
          DYLD_LIBRARY_PATH=$CONDA_PREFIX/lib:$DYLD_LIBRARY_PATH ctest --output-on-failure || true

      - name: Package binaries
        shell: bash -l {0}
        run: |
          mkdir -p dist
          # Bundle required libraries
          cp $CONDA_PREFIX/lib/libonnxruntime.${{ env.ONNX_VERSION }}.dylib bin/
          ln -sf libonnxruntime.${{ env.ONNX_VERSION }}.dylib bin/libonnxruntime.dylib
          cp $CONDA_PREFIX/lib/libduckdb.dylib bin/
          # Update library paths
          for bin in bin/chitta bin/chittad bin/chitta_migrate bin/chitta_import bin/duckdb_test; do
            install_name_tool -add_rpath @executable_path $bin 2>/dev/null || true
            # Fix libonnxruntime reference
            install_name_tool -change @rpath/libonnxruntime.${{ env.ONNX_VERSION }}.dylib @executable_path/libonnxruntime.${{ env.ONNX_VERSION }}.dylib $bin 2>/dev/null || true
            # Fix libduckdb reference
            install_name_tool -change @rpath/libduckdb.dylib @executable_path/libduckdb.dylib $bin 2>/dev/null || true
          done
          # Create tarball
          tar -czvf dist/chitta-${{ matrix.arch }}.tar.gz -C bin .

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: chitta-${{ matrix.arch }}
          path: dist/chitta-${{ matrix.arch }}.tar.gz

  release:
    needs: [build-linux, build-macos, models]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release
          merge-multiple: true

      - name: List release files
        run: ls -la release/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          generate_release_notes: true
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
