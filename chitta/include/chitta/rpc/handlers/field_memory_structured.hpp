// Included into FieldRpcHandler class body — declarations only.
// Bodies live in chitta/src/handlers/field_memory_structured.cpp.

    ToolResult tool_structured_recall(const json& params);
    ToolResult tool_ask(const json& params);
