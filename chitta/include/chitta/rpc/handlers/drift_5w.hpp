// Included into FieldRpcHandler class body — declarations only.
// Bodies live in chitta/src/handlers/drift_5w.cpp.

    ToolResult tool_5w_search(const json& params);
    ToolResult tool_recall_ucb1(const json& params);
