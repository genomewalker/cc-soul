// Included into FieldRpcHandler class body — declarations only.
// Bodies live in chitta/src/handlers/field_contradiction.cpp.

    ToolResult tool_why_active(const json& params);
    ToolResult tool_what_superseded(const json& params);
    ToolResult tool_show_conflicts(const json& params);
    ToolResult tool_detect_contradictions(const json& params);
    ToolResult tool_resolve_contradiction(const json& params);
    ToolResult tool_scan_contradictions(const json& params);
    ToolResult tool_cross_harness_conflicts(const json& params);
