// Included into FieldRpcHandler class body — declarations only.
// Bodies live in chitta/src/handlers/field_skill.cpp.

    ToolResult tool_skill_upload(const json& params);
    ToolResult tool_skill_read(const json& params);
    ToolResult tool_skill_list();
    ToolResult tool_skill_search(const json& params);
    ToolResult tool_skill_deprecate(const json& params);
