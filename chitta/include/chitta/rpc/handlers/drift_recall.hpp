// Included into FieldRpcHandler class body — declarations only.
// Bodies live in chitta/src/handlers/drift_recall.cpp.

    ToolResult tool_cooccurrence_graph(const json& params);
    ToolResult tool_labile_memories_top(const json& params);
