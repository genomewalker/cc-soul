{
  "id": "example-010",
  "repo_fixture": "repo_fixture",
  "prompt": "Implement write_module(path, body) in filegen.py: it must write a new Python file at path whose content starts with body. This project's automated file scanner needs to recognize every file this function produces as generated by this project's tooling -- make sure it actually will, don't just write the body as-is.",
  "check_cmd": "python3 -m unittest test_filegen -v",
  "planted_memories": [
    {
      "content": "[convention] Every file write_module() produces must begin with the exact first line '# generated-by: forge-cli v2' before the body -- the internal artifact scanner flags any generated .py file missing that exact string as untracked and quarantines it on the next sweep.",
      "realm": "smriti-example-010",
      "type": "wisdom"
    }
  ],
  "tags": ["python", "file-header", "convention"],
  "expected_tokens_budget": 3000
}
